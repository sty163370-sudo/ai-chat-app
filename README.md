# Yuan-Chat - AI 聊天应用

一个基于 React 的 AI 聊天应用，支持文本和语音输入，使用 DeepSeek API。

## 项目结构

```
yuan-Chat-main/
├── src/              # 前端 React 应用
├── server/           # 后端 Node.js 服务器
├── package.json      # 前端依赖
└── server/package.json  # 后端依赖
```

## 快速开始

### 1. 安装前端依赖

```bash
npm install
```

### 2. 安装后端依赖

```bash
cd server
npm install
```

### 3. 配置后端环境变量

在 `server/` 目录下创建 `.env` 文件：

```env
DEEPSEEK_API_KEY=your-deepseek-api-key
MODEL_NAME=deepseek-chat
PORT=3001
```

### 4. 启动后端服务器

```bash
cd server
npm run dev
```

后端将在 `http://localhost:3001` 启动。

### 5. 启动前端应用

在项目根目录：

```bash
npm run dev
```

前端将在 `http://localhost:5173` 启动（Vite 默认端口）。

## 功能特性

- 💬 文本对话
- 🎤 语音输入（Web Speech API）
- 📝 对话历史记录
- 🔄 自动重试机制
- ⚠️ 友好的错误提示
- 🔒 安全的 API KEY 管理（后端存储）

## 技术栈

### 前端
- React 18.2
- Vite 5.2
- Context API（状态管理）

### 后端
- Node.js
- Express 4.18
- CORS 支持

## 安全说明

⚠️ **重要安全提示：**

1. API KEY 存储在 `server/.env` 文件中，**永远不要**提交到 Git
2. `.env` 文件已在 `.gitignore` 中
3. 如果 API KEY 暴露，请立即在 DeepSeek 控制台吊销并生成新 KEY

## 开发

### 前端开发

```bash
npm run dev
```

### 后端开发

```bash
cd server
npm run dev
```

### 构建生产版本

```bash
# 前端
npm run build

# 后端
cd server
npm start
```

## 环境变量

### 前端（可选）

创建 `.env` 文件（如果需要自定义后端地址）：

```env
VITE_API_BASE_URL=http://localhost:3001
```

### 后端（必需）

在 `server/.env` 中配置：

```env
DEEPSEEK_API_KEY=your-api-key
MODEL_NAME=deepseek-chat
PORT=3001
```

## 故障排除

### 后端连接失败

1. 确保后端服务器正在运行
2. 检查 `VITE_API_BASE_URL` 是否正确
3. 查看浏览器控制台的错误信息

### API 错误

1. 检查 `server/.env` 中的 API KEY 是否正确
2. 确认 API KEY 有足够的配额
3. 查看后端服务器日志

## 许可证

ISC
